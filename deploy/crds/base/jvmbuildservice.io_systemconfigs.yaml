
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.2
  creationTimestamp: null
  name: systemconfigs.jvmbuildservice.io
spec:
  group: jvmbuildservice.io
  names:
    kind: SystemConfig
    listKind: SystemConfigList
    plural: systemconfigs
    singular: systemconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SystemConfig TODO provide godoc description
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              builders:
                additionalProperties:
                  properties:
                    image:
                      type: string
                    priority:
                      type: integer
                    tag:
                      type: string
                  type: object
                type: object
              gitCloneImage:
                type: string
              postBuildTasks:
                items:
                  properties:
                    name:
                      type: string
                    params:
                      items:
                        description: Param declares an ArrayOrString to use for the
                          parameter called name.
                        properties:
                          name:
                            type: string
                          value:
                            description: ArrayOrString is a type that can hold a single
                              string or string array. Used in JSON unmarshalling so
                              that a single JSON field can accept either an individual
                              string or an array of strings.
                            properties:
                              arrayVal:
                                items:
                                  type: string
                                type: array
                              stringVal:
                                type: string
                              type:
                                description: ParamType indicates the type of an input
                                  parameter; Used to distinguish between a single
                                  string and an array of strings.
                                type: string
                            required:
                            - arrayVal
                            - stringVal
                            - type
                            type: object
                        required:
                        - name
                        - value
                        type: object
                      type: array
                    taskRef:
                      properties:
                        bundle:
                          type: string
                        name:
                          type: string
                      type: object
                    workspaces:
                      description: Workspaces is a list of WorkspaceBindings from
                        volumes to workspaces.
                      items:
                        description: WorkspacePipelineTaskBinding describes how a
                          workspace passed into the pipeline should be mapped to a
                          task's declared workspace.
                        properties:
                          name:
                            description: Name is the name of the workspace as declared
                              by the task
                            type: string
                          subPath:
                            description: SubPath is optionally a directory on the
                              volume which should be used for this binding (i.e. the
                              volume will be mounted at this sub directory).
                            type: string
                          workspace:
                            description: Workspace is the name of the workspace declared
                              by the pipeline
                            type: string
                        required:
                        - name
                        - workspace
                        type: object
                      type: array
                  type: object
                type: array
              preBuildTasks:
                items:
                  properties:
                    name:
                      type: string
                    params:
                      items:
                        description: Param declares an ArrayOrString to use for the
                          parameter called name.
                        properties:
                          name:
                            type: string
                          value:
                            description: ArrayOrString is a type that can hold a single
                              string or string array. Used in JSON unmarshalling so
                              that a single JSON field can accept either an individual
                              string or an array of strings.
                            properties:
                              arrayVal:
                                items:
                                  type: string
                                type: array
                              stringVal:
                                type: string
                              type:
                                description: ParamType indicates the type of an input
                                  parameter; Used to distinguish between a single
                                  string and an array of strings.
                                type: string
                            required:
                            - arrayVal
                            - stringVal
                            - type
                            type: object
                        required:
                        - name
                        - value
                        type: object
                      type: array
                    taskRef:
                      properties:
                        bundle:
                          type: string
                        name:
                          type: string
                      type: object
                    workspaces:
                      description: Workspaces is a list of WorkspaceBindings from
                        volumes to workspaces.
                      items:
                        description: WorkspacePipelineTaskBinding describes how a
                          workspace passed into the pipeline should be mapped to a
                          task's declared workspace.
                        properties:
                          name:
                            description: Name is the name of the workspace as declared
                              by the task
                            type: string
                          subPath:
                            description: SubPath is optionally a directory on the
                              volume which should be used for this binding (i.e. the
                              volume will be mounted at this sub directory).
                            type: string
                          workspace:
                            description: Workspace is the name of the workspace declared
                              by the pipeline
                            type: string
                        required:
                        - name
                        - workspace
                        type: object
                      type: array
                  type: object
                type: array
              quota:
                type: string
            type: object
          status:
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
